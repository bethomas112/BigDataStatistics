#ifndef DIST_KERNEL_H
#define DIST_KERNEL_H

#include <map>
#include <string>
#include <vector>
#include <stdio.h>

using namespace std;

void sortAndPrintMap(map<string, int> words, FILE *outfile);

#endif 
